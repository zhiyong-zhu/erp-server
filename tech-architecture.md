# 全渠道企业资源管理系统（ERP）技术架构说明文档

> 版本：v1.0  
> 更新日期：2026-05-20  
> 文档状态：初稿

---

## 一、文档概述

### 1.1 文档目的

本文档详细描述全渠道ERP系统的技术架构设计，包括整体架构、技术选型、各层设计原则及关键技术实现方案。面向技术团队成员、架构评审人员和运维团队。

### 1.2 读者对象

| 角色 | 关注重点 |
|------|---------|
| 架构师 | 整体架构、技术选型决策 |
| 后端开发 | 后端架构、中间件、数据库设计 |
| 前端开发 | 前端架构、多端复用策略 |
| 运维工程师 | 部署架构、监控、CI/CD |
| 项目经理 | 技术风险、依赖关系 |

### 1.3 术语表

| 术语 | 说明 |
|------|------|
| BOM | Bill of Materials，物料清单 |
| MRP | Material Requirements Planning，物料需求计划 |
| RBAC | Role-Based Access Control，基于角色的访问控制 |
| SKU | Stock Keeping Unit，库存量单位 |
| SLA | Service Level Agreement，服务等级协议 |
| JSONB | PostgreSQL 的二进制 JSON 类型 |
| CTE | Common Table Expression，公用表表达式 |

---

## 二、架构设计原则

| 原则 | 说明 |
|------|------|
| **分层解耦** | 前后端分离，各层职责清晰，通过 API 契约通信 |
| **模块化单体** | 初期采用模块化单体架构，降低运维复杂度，预留微服务拆分能力 |
| **多端复用** | 前端以 React 为统一技术栈，最大化代码共享 |
| **安全纵深** | 认证、授权、传输加密、数据加密多层防护 |
| **可观测性** | 全链路日志、指标监控、告警体系 |
| **渐进演进** | 架构支持从单体到分布式的平滑演进 |

---

## 三、整体架构

### 3.1 系统架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                           客户端层                                    │
│                                                                      │
│  ┌──────────────────┐  ┌────────────┐  ┌───────────────────────┐   │
│  │  桌面客户端        │  │  Web 端    │  │  移动端 App            │   │
│  │  Tauri + React    │  │  React SPA │  │  React Native          │   │
│  │  (Windows/macOS)  │  │            │  │  (Android + iOS)       │   │
│  └────────┬─────────┘  └─────┬──────┘  └───────────┬────────────┘   │
│           │                   │                     │                 │
│           └───────────────────┼─────────────────────┘                │
│                               │                                      │
│                        HTTPS + JWT Token                             │
└───────────────────────────────┼──────────────────────────────────────┘
                                │
┌───────────────────────────────┼──────────────────────────────────────┐
│                         接入层                                        │
│                               │                                      │
│                    ┌──────────┴──────────┐                           │
│                    │    Nginx 反向代理     │                           │
│                    │  (负载均衡/SSL终止/    │                           │
│                    │   静态资源/限流)       │                           │
│                    └──────────┬──────────┘                           │
│                               │                                      │
└───────────────────────────────┼──────────────────────────────────────┘
                                │
┌───────────────────────────────┼──────────────────────────────────────┐
│                         应用服务层                                     │
│                               │                                      │
│  ┌────────────────────────────┴─────────────────────────────────┐   │
│  │              Spring Boot 3.x 模块化单体应用                     │   │
│  │                                                               │   │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐   │   │
│  │  │ 产品模块   │ │ 库存模块   │ │ 销售模块   │ │ 采购模块   │   │   │
│  │  └───────────┘ └───────────┘ └───────────┘ └───────────┘   │   │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐   │   │
│  │  │ 生产模块   │ │ 财务模块   │ │ 人事模块   │ │ 集成模块   │   │   │
│  │  └───────────┘ └───────────┘ └───────────┘ └───────────┘   │   │
│  │                                                               │   │
│  │  横切关注点: Security | AOP日志 | 异常处理 | 数据校验          │   │
│  └───────────────────────────────────────────────────────────────┘   │
│                                                                      │
└───────────────────────────────┬──────────────────────────────────────┘
                                │
┌───────────────────────────────┼──────────────────────────────────────┐
│                         数据与中间件层                                 │
│                               │                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐    │
│  │PostgreSQL│  │  Redis 7  │  │ RabbitMQ │  │ RustFS (对象存储) │    │
│  │  15+     │  │  集群     │  │  消息队列 │  │ 文件/图片存储     │    │
│  │  (主库)  │  │  (缓存)   │  │          │  │                  │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────────┘    │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
                                │
┌───────────────────────────────┼──────────────────────────────────────┐
│                         外部集成层                                     │
│                                                                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │ 电商平台 API  │  │ 电子发票平台  │  │ 物流平台 API  │              │
│  │ (淘宝/京东/   │  │ (税务UKey/    │  │ (快递100/     │              │
│  │  拼多多/抖音) │  │  诺诺/百旺)   │  │  菜鸟/京东)   │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

### 3.2 部署架构

```
┌─────────────────────────────────────────────────────┐
│                  生产环境 (Docker Compose)             │
│                                                      │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐            │
│  │  Nginx  │  │  App-1  │  │  App-2  │  (可水平扩展) │
│  │  :80/443│──│  :8080  │  │  :8080  │            │
│  └─────────┘  └─────────┘  └─────────┘            │
│                                                      │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐            │
│  │  PgSQL  │  │  Redis  │  │RabbitMQ │            │
│  │  :5432  │  │  :6379  │  │  :5672  │            │
│  └─────────┘  └─────────┘  └─────────┘            │
│                                                      │
│  ┌─────────┐  ┌─────────────────────┐              │
│  │ RustFS  │  │  Prometheus+Grafana │              │
│  │  :9000  │  │  :9090 / :3000      │              │
│  └─────────┘  └─────────────────────┘              │
└─────────────────────────────────────────────────────┘
```

---

## 四、后端架构设计

### 4.1 技术栈

| 组件 | 版本 | 用途 |
|------|------|------|
| Java | 17 LTS | 运行环境 |
| Spring Boot | 3.2+ | 基础框架 |
| Spring Security | 6.x | 认证授权 |
| MyBatis-Plus | 3.5+ | ORM框架 |
| PostgreSQL | 15+ | 主数据库 |
| Redis | 7.x | 缓存、会话、分布式锁 |
| RabbitMQ | 3.12+ | 异步消息 |
| XXL-JOB | 2.4+ | 分布式定时任务 |
| Flowable | 7.1+ | 审批流程引擎（BPMN 2.0） |
| RustFS | 最新 | 对象存储 |
| Flyway | 9.x | 数据库版本管理 |
| Springdoc OpenAPI | 2.x | API 文档 |
| MapStruct | 1.5+ | 对象映射 |
| Hutool | 5.8+ | 工具库 |

### 4.2 分层架构

```
┌─────────────────────────────────────────┐
│  Controller 层 (REST API 入口)           │
│  - 参数校验 (@Valid)                     │
│  - 统一响应封装                           │
│  - Swagger 注解                          │
├─────────────────────────────────────────┤
│  Service 层 (业务逻辑)                    │
│  - 事务管理 (@Transactional)             │
│  - 业务规则校验                           │
│  - 跨模块协调                            │
├─────────────────────────────────────────┤
│  Manager 层 (通用业务处理)                │
│  - 缓存操作                              │
│  - 第三方服务调用                         │
│  - 消息发送                              │
├─────────────────────────────────────────┤
│  Mapper/Repository 层 (数据访问)          │
│  - MyBatis-Plus CRUD                     │
│  - 自定义 SQL (复杂查询)                  │
│  - 分页查询                              │
├─────────────────────────────────────────┤
│  Entity 层 (数据模型)                     │
│  - DO (Database Object)                  │
│  - DTO (Data Transfer Object)            │
│  - VO (View Object)                      │
│  - Query (查询条件)                       │
└─────────────────────────────────────────┘
```

### 4.3 模块化设计

采用 Maven 多模块项目结构，按业务域拆分：

```
erp-server/
├── pom.xml                        # 父 POM，统一依赖版本
├── erp-common/                    # 公共模块
│   ├── erp-common-core/           # 核心工具、统一响应、异常定义
│   ├── erp-common-security/       # 安全框架封装
│   ├── erp-common-mybatis/        # MyBatis-Plus 配置
│   └── erp-common-redis/          # Redis 工具封装
├── erp-system/                    # 系统管理 (用户、角色、权限、字典)
├── erp-product/                   # 产品管理 (产品、分类、BOM、包装、标签)
├── erp-material/                  # 原料管理 (原料、供应商)
├── erp-inventory/                 # 库存管理 (入库、出库、盘点、调拨)
├── erp-sales/                     # 销售管理 (客户、订单、退货)
├── erp-purchase/                  # 采购管理 (采购单、到货、应付)
├── erp-production/                # 生产管理 (工艺、工单、MRP)
├── erp-finance/                   # 财务管理 (价格、发票、报税)
├── erp-hr/                        # 人事管理 (员工、考勤、审批)
├── erp-integration/               # 外部集成 (电商、物流、发票平台)
└── erp-admin/                     # 启动模块 (聚合所有模块，打包入口)
```

### 4.4 关键设计决策

#### 4.4.1 单体 vs 微服务

**当前阶段采用模块化单体**，原因：
- 团队规模小，微服务运维成本高
- ERP 业务模块间耦合度高（如订单涉及库存、财务、物流）
- 单体内模块间调用无网络开销，事务管理简单

**未来拆分策略**：
- 当单模块代码量 > 5万行或团队 > 10人时考虑拆分
- 优先拆分"集成模块"（电商对接天然独立）
- 通过 RabbitMQ 事件驱动解耦做前期准备

#### 4.4.2 API 设计规范

```
基础路径: /api/v1/{module}/{resource}
示例:
  GET    /api/v1/product/products          # 产品列表（分页）
  GET    /api/v1/product/products/{id}     # 产品详情
  POST   /api/v1/product/products          # 创建产品
  PUT    /api/v1/product/products/{id}     # 更新产品
  DELETE /api/v1/product/products/{id}     # 删除产品
  POST   /api/v1/product/products/export   # 导出
```

**统一响应格式：**
```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "timestamp": 1716220800000
}
```

**错误码体系：**
| 范围 | 模块 |
|------|------|
| 10000-19999 | 系统级错误 |
| 20000-29999 | 产品模块 |
| 30000-39999 | 库存模块 |
| 40000-49999 | 销售模块 |
| 50000-59999 | 财务模块 |
| 60000-69999 | 集成模块 |

#### 4.4.3 认证与授权

```
认证流程:
用户登录 → 验证凭据 → 签发 JWT (Access Token + Refresh Token)
                                    │
                        Access Token: 有效期 2h
                        Refresh Token: 有效期 7d, 存 Redis

API 请求:
Request → Nginx → Spring Security Filter → JWT 验证
    → 解析用户信息 → 权限校验 (RBAC) → Controller
```

**权限模型**：
- 基于 RBAC（用户-角色-权限）
- 支持数据权限（部门级、个人级）
- 支持字段级权限（敏感字段如成本价仅管理员可见）
- 操作审计日志（who/when/what/where）

#### 4.4.4 缓存策略

| 数据类型 | 缓存方式 | TTL | 更新策略 |
|----------|---------|-----|---------|
| 数据字典 | Redis String | 24h | 修改时主动失效 |
| 用户权限 | Redis Hash | 2h | 角色变更时刷新 |
| 产品信息 | Redis String | 1h | Cache Aside |
| 库存数量 | Redis + DB | 实时 | 写穿透 |
| 会话Token | Redis String | 2h/7d | 登出时删除 |

#### 4.4.5 消息队列使用场景

| 场景 | Exchange | 说明 |
|------|----------|------|
| 电商订单同步 | order.sync | 平台推送 → 消费入库 |
| 库存变动通知 | inventory.change | 库存变化 → 电商同步/预警 |
| 发票开具 | invoice.issue | 异步开票 |
| 操作日志 | audit.log | 异步写入日志表 |
| 报表生成 | report.generate | 耗时报表异步生成 |

---

## 五、前端架构设计

### 5.1 多端复用策略

采用 **pnpm Monorepo** 管理前端项目：

```
erp-frontend/
├── pnpm-workspace.yaml
├── packages/
│   ├── shared/                    # 共享层 (三端通用)
│   │   ├── api/                   # API 请求层 (Axios 封装)
│   │   ├── types/                 # TypeScript 类型定义
│   │   ├── store/                 # Zustand 状态管理
│   │   ├── hooks/                 # 通用业务 Hooks
│   │   ├── utils/                 # 工具函数
│   │   └── constants/             # 常量定义
│   ├── ui-web/                    # Web 端组件库 (基于 Ant Design)
│   └── ui-mobile/                 # 移动端组件库 (基于 Tamagui)
├── apps/
│   ├── web/                       # Web 端应用
│   │   ├── src/
│   │   │   ├── pages/             # 页面组件
│   │   │   ├── layouts/           # 布局组件
│   │   │   ├── router/            # 路由配置
│   │   │   └── main.tsx
│   │   └── vite.config.ts
│   ├── desktop/                   # Tauri 桌面端
│   │   ├── src/                   # 复用 web 页面 + 桌面特有功能
│   │   ├── src-tauri/             # Rust 本地能力
│   │   └── tauri.conf.json
│   └── mobile/                    # React Native 移动端
│       ├── src/
│       │   ├── screens/           # 移动端页面
│       │   ├── navigation/        # 导航配置
│       │   └── native-modules/    # 原生模块桥接
│       └── app.json
└── tools/
    └── codegen/                   # API 类型自动生成工具
```

### 5.2 Web 端技术栈

| 组件 | 版本 | 用途 |
|------|------|------|
| React | 18.x | UI 框架 |
| TypeScript | 5.x | 类型安全 |
| Vite | 5.x | 构建工具 |
| Ant Design | 5.x | UI 组件库 |
| React Router | 6.x | 路由管理 |
| Zustand | 4.x | 客户端状态管理 |
| TanStack Query | 5.x | 服务端状态 & 缓存 |
| ECharts | 5.x | 数据可视化 |
| Axios | 1.x | HTTP 客户端 |
| dayjs | 1.x | 日期处理 |
| react-to-print | - | 浏览器打印 |

### 5.3 桌面端（Tauri）架构

```
┌──────────────────────────────────────────┐
│            React 前端 (与Web端共享)         │
│  ┌──────────────────────────────────┐    │
│  │  业务页面、组件、状态管理            │    │
│  │  (直接复用 @erp/shared + @erp/web) │    │
│  └────────────────┬─────────────────┘    │
│                   │                       │
│      ┌────────────┴────────────┐         │
│      │ HTTP API (Spring Boot)  │  invoke │
│      │ (所有业务请求)           │  (本地)  │
│      └─────────────────────────┘         │
│                                    │      │
│  ┌─────────────────────────────────┴──┐  │
│  │         Tauri Rust 后端              │  │
│  │  - print_label: 本地打印驱动        │  │
│  │  - local_cache: 离线数据缓存        │  │
│  │  - system_info: 获取设备信息        │  │
│  │  - auto_update: 应用自动更新        │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
```

**Tauri Rust 端职责边界（严格最小化）：**
- 本地标签打印（直驱打印机）
- 离线数据缓存（SQLite 本地存储）
- 系统通知（托盘图标、桌面通知）
- 自动更新检查
- 本地文件操作（导入/导出 Excel）

### 5.4 移动端架构

**核心场景（移动端优先功能）：**
- 扫码出入库
- 订单状态查看与审批
- 库存预警推送
- 简易报表查看

**与 Web 端的差异：**
- UI 组件库不同（Tamagui 替代 Ant Design）
- 导航方式不同（Stack/Tab Navigation 替代 React Router）
- 增加原生能力（摄像头扫码、推送通知）
- 页面布局针对小屏优化

---

## 六、数据库架构设计

### 6.1 PostgreSQL 特性利用

| 特性 | 使用场景 |
|------|---------|
| **JSONB** | 产品动态规格、标签模板配置、电商平台原始数据 |
| **窗口函数** | 库存周转率、销售排行、财务汇总报表 |
| **CTE (WITH)** | BOM 递归展开、组织架构树查询 |
| **全文检索 (tsvector)** | 产品搜索、客户搜索（配合 zhparser 中文分词） |
| **分区表** | 库存流水表按月分区、操作日志表按月分区 |
| **行级锁 (FOR UPDATE)** | 库存扣减原子性保证 |
| **数组类型** | 权限标识符存储、标签列表 |
| **生成列** | 自动计算字段（如含税金额 = 不含税 × 税率） |

### 6.2 数据库规范

- 表名：小写下划线，如 `sale_order`
- 字段名：小写下划线，如 `created_at`
- 主键：UUID（`gen_random_uuid()`），避免自增 ID 暴露业务量
- 时间字段：统一使用 `TIMESTAMPTZ`（带时区）
- 软删除：`deleted` BOOLEAN + `deleted_at` TIMESTAMPTZ
- 审计字段：每表必含 `created_by`, `created_at`, `updated_by`, `updated_at`
- 索引命名：`idx_{表名}_{字段名}`
- 外键命名：`fk_{表名}_{关联表名}`

### 6.3 读写分离与扩展

- **初期**：单节点 PostgreSQL + 定期备份
- **中期**：主从复制，读操作分流到从库
- **远期**：按业务域分库（如财务独立库）

---

## 七、安全架构

### 7.1 网络安全

- 全站 HTTPS（TLS 1.3），Nginx 终止 SSL
- 内网服务间通信可用 HTTP（Docker 网络隔离）
- API 限流：Nginx `limit_req_zone` + 应用层令牌桶

### 7.2 应用安全

| 威胁 | 防护措施 |
|------|---------|
| SQL 注入 | MyBatis-Plus 参数绑定，禁止字符串拼接 SQL |
| XSS | 前端输入消毒，后端输出编码，CSP 头 |
| CSRF | JWT + SameSite Cookie + 自定义Header校验 |
| 越权 | 数据权限过滤（部门/个人），接口粒度鉴权 |
| 密码泄露 | BCrypt 加密存储，密码强度策略 |
| Token 泄露 | 短有效期 + Refresh Token 轮转 + 异常登录检测 |

### 7.3 数据安全

- 敏感字段加密存储（AES-256）：电商平台 Token、银行账号
- 数据库连接池加密
- 定期自动备份（pg_dump → RustFS/OSS），保留 30 天
- 操作审计日志不可篡改（追加写入）

### 7.4 合规要求

- 符合《个人信息保护法》：员工信息、客户信息最小化收集
- 数据导出需权限控制与日志记录
- 支持数据删除请求（GDPR right to erasure）

---

## 八、可观测性设计

### 8.1 日志体系

```
┌───────────────┐    ┌──────────┐    ┌──────────────────┐
│ Spring Boot   │───▶│ Filebeat │───▶│ Elasticsearch    │
│ (Logback JSON)│    │          │    │ + Kibana         │
└───────────────┘    └──────────┘    └──────────────────┘
```

- 日志格式：JSON 结构化（traceId, userId, module, action）
- 日志级别：生产环境 INFO，可动态调整
- 业务日志与系统日志分离

### 8.2 指标监控

| 监控项 | 工具 | 告警阈值 |
|--------|------|---------|
| JVM 指标 | Prometheus + Micrometer | 堆内存 > 80% |
| API 响应时间 | Prometheus | P99 > 3s |
| 数据库连接池 | HikariCP metrics | 活跃连接 > 80% |
| 消息队列堆积 | RabbitMQ Exporter | 堆积 > 10000 |
| 磁盘空间 | Node Exporter | 使用率 > 85% |
| 业务指标 | 自定义 metrics | 订单同步失败率 > 5% |

### 8.3 告警通知

- Grafana Alerting → 企业微信/钉钉群机器人
- 分级告警：P0 电话 / P1 即时消息 / P2 邮件

---

## 九、性能设计

### 9.1 性能目标

| 指标 | 目标值 |
|------|--------|
| API 平均响应时间 | < 200ms |
| API P99 响应时间 | < 1s |
| 并发用户数 | 500+ |
| 系统可用性 | 99.5% (月度) |
| 数据库查询 | 单查询 < 100ms |
| 页面首屏加载 | < 2s |

### 9.2 性能优化策略

- **数据库层**：合理索引、EXPLAIN 分析慢查询、连接池调优
- **缓存层**：热点数据 Redis 缓存、本地缓存（Caffeine）二级缓存
- **应用层**：异步处理（@Async）、批量操作、分页查询
- **前端层**：代码分割（React.lazy）、虚拟滚动、图片懒加载
- **网络层**：Gzip 压缩、CDN 静态资源、HTTP/2

---

## 十、CI/CD 与部署

### 10.1 流水线设计

```
代码提交 → Git Hook → CI Pipeline
    │
    ├── 代码质量检查 (SonarQube)
    ├── 单元测试 (JUnit 5 + Mockito)
    ├── 构建打包 (Maven + Vite)
    ├── 集成测试 (Testcontainers)
    ├── Docker 镜像构建
    └── 部署
        ├── dev 环境：自动部署
        ├── staging 环境：手动触发
        └── production 环境：审批后部署
```

### 10.2 环境规划

| 环境 | 用途 | 部署方式 |
|------|------|---------|
| local | 开发调试 | Docker Compose (轻量) |
| dev | 联调测试 | Docker Compose (完整) |
| staging | 预发布验证 | 与生产同配置 |
| production | 正式环境 | Docker Compose / K8s |

### 10.3 桌面端/移动端发布

- **桌面端**：GitHub Actions 构建 → 生成安装包 → 应用内自动更新（Tauri Updater）
- **移动端**：EAS Build → 审核 → 应用商店发布（支持 CodePush 热更新）
- **灰度策略**：按用户百分比逐步放量

---

## 十一、技术风险与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| 电商平台 API 变更 | 高 | 中 | 适配层版本化，监控 API 通知，预留 2 周缓冲 |
| Tauri 生态不成熟 | 中 | 中 | 关键功能（打印）提前 PoC，准备 Electron 降级方案 |
| PostgreSQL 运维经验不足 | 中 | 高 | 使用云数据库（阿里云 RDS/华为 GaussDB），定期演练恢复 |
| 三端同步数据一致性 | 中 | 中 | 服务端权威数据源 + 乐观锁 + 版本号比对 |
| AI 生成代码质量不稳定 | 低 | 低 | Code Review 必须人工，单测覆盖率 > 70% |
| 大促流量峰值 | 低 | 高 | RabbitMQ 削峰、限流降级、提前压测 |

---

## 十二、技术选型对比总结

| 维度 | 本方案 (Spring+React+Tauri) | 全 Rust 方案 | 全 JavaScript 方案 (Node+Electron) |
|------|----------------------------|-------------|-----------------------------------|
| 后端性能 | 良好（满足 ERP 需求） | 极高 | 一般 |
| 开发效率 | 极高（成熟生态+AI友好） | 中等 | 高 |
| 桌面端体积 | 小 (<10MB) | 小 (<10MB) | 大 (>50MB) |
| 多端代码复用 | 高（React Monorepo） | 低 | 高 |
| 国内招聘难度 | 低 | 高 | 低 |
| 运维复杂度 | 中 | 高 | 中 |
| 长期可维护性 | 高 | 中 | 中 |
